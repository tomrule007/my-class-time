// const fetch = require('node-fetch');

// Do some fetching/scraping to retrieve classes that will be passed to the generation algorithm
const retrieveClasses = async () => {
  const results = [];

  return results;
};

module.exports = retrieveClasses;
