// inputs is an object containing all the whitelist/blacklist arguments from the user
// classes is an array containing all available classes. Unsure of the expected format

// TODO Decide on format of inputs and classes
const generateSchedule = (
  inputs = {
    whitelist: {},
    blacklist: {},
  },
  classes = [],
) => {
  console.log(inputs);
  console.log(classes);

  // TODO Decide on the format of output
  return {};
};

module.exports = generateSchedule;
